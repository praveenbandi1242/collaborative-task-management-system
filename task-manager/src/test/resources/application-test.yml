spring:
  datasource:
    url: jdbc:postgresql://localhost:5437/taskmanager
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update

jwt:
  secret: test-secret-key-for-github-actions-only
  expiration: 86400000